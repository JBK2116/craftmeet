craftmeet.live {
    encode gzip

    handle_path /api/v1/* {
        reverse_proxy backend:8000
    }

    handle {
        root * /srv
        try_files {path} {path}/ /index.html
        file_server
    }
}
